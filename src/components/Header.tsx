import styles from "@/styles/Header.module.css"

const Header = () => {
  return (
    <header className={styles.PageHeader}>
      <h1 className={styles.PageTitle}>Monthly Spending App</h1>
    </header>
  )
}

export default Header
